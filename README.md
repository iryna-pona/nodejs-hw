# NoteHub — Backend

Backend part of NoteHub, a personal notes management application.

## 🔹 Overview
This project provides the backend API for NoteHub.  
It handles user authentication, CRUD operations for notes, and data storage in MongoDB.  
The API is designed to work with the frontend (React + Next.js) and uses cookies for authentication.

## 🛠 Technologies
Node.js | Express | MongoDB | Mongoose | REST API | Cookies | JavaScript (ES6+) | dotenv

## 📂 Features
- User registration and login
- Authentication using cookies
- CRUD operations for notes (Create, Read, Update, Delete)
- RESTful API endpoints
- Connection to MongoDB database
- Error handling and validation

## 🚀 Endpoints
Here are some main endpoints (as examples):
- `POST /api/auth/register` — register a new user
- `POST /api/auth/login` — login user and set cookie
- `GET /api/notes` — get all notes for the logged-in user
- `POST /api/notes` — create a new note
- `PATCH /api/notes/:id` — update a note
- `DELETE /api/notes/:id` — delete a note

*(You can add more endpoints if you want to describe all available routes.)*

## 🔗 Frontend
This backend works with the frontend repository:  
[NoteHub Frontend](https://github.com/iryna-pona/notehub-frontend)

## 👩‍💻 Role
Individual project — Backend Developer

## 📫 Contact
[LinkedIn](https://www.linkedin.com/in/iryna-pona) | [Email](mailto:youremail@gmail.com)

